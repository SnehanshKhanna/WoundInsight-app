name: wound_insight_app
description: "WoundInsight – Automated Multi-Task Wound Diagnostic, Morphometric & Explainability AI System"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State Management (plain Riverpod, no code gen for providers)
  flutter_riverpod: ^2.5.1

  # Networking & Serialization
  dio: ^5.4.3+1
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

  # Storage & Utilities
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.2.3
  intl: ^0.19.0

  # UI, Charts & Media
  image_picker: ^1.1.2
  fl_chart: ^0.68.0
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # Code Generation (Models only)
  build_runner: ^2.4.11
  freezed: ^2.5.2
  json_serializable: ^6.8.0

  # Testing
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
